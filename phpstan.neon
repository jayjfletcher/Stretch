includes:
    - phpstan-baseline.neon

# Extensions (larastan, phpstan-phpunit, deprecation-rules) are loaded
# automatically via phpstan/extension-installer.
parameters:
    level: 5
    paths:
        - src
        - config
        - tests
